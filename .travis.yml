language: node_js
node_js:
  - "iojs"
  - "lts/*"

before_script:
  - npm i -g typescript

script:
  - cd momentum
  - npm install
  - npm run build
  - npm test

cache:
  directories:
    - "momentum/node_modules"
